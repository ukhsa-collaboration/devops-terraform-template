name: "[Analysis] Check Terraform Code"
on:
  pull_request:
    branches:
      - main

jobs:
  dev_check:
    name: Check Development Terraform
    uses: UKHSA-Internal/devops-github-reusable-workflows/.github/workflows/terraform-code-check.yml@main
    permissions:
      packages: read
      contents: read
      security-events: write
      statuses: write
      checks: write
